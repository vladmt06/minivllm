[project]
name = "minivllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.25.1",
    "numpy>=2.5.1",
    "pytest>=9.1.1",
    "safetensors>=0.8.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
]
